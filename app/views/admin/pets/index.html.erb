<%= render 'admin/sessions/header' %>
<h2>会員一覧</h2>
<h4>名前</h4>
<% @users.each do |user|%>
    <%= user.updated_at.strftime('%Y/%m/%d %H:%M:%S') %>
    <%= link_to user.name,admin_pet_path(user.pet.id) %><br>
<% end %>

